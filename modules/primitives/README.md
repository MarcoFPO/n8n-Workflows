# Tier 1: Primitives

Low-Level-Operationen — kleinste wiederverwendbare Bausteine.

| Modul | Funktion |
|-------|----------|
| `http-request-wrapper.json` | Generischer HTTP-Aufruf mit Fehlerbehandlung |
| `ssh-command-executor.json` | SSH-Befehl auf Ziel-Host ausführen |
| `json-schema-validator.json` | JSON-Eingabe gegen Schema validieren |
| `result-formatter.json` | Ausgabe in Standardformat bringen |
| `netbox-credential-resolver.json` | NetBox-API-Credentials auflösen |
| `ki-executer/` | Claude-Code-API-Wrapper (Subverzeichnis) |
